import React from "react";

const SubmitButton = () => {
  return (
    <button
      type="submit"
      className={`w-48 p-3 font-shanti bg-primary text-white text-sm rounded-3xl hover:scale-105`}
    >
      Submit
    </button>
  );
};

export default SubmitButton;
